redshift_profile:
  target: dev
  outputs:
    dev:
      type: redshift
      threads: 1
      host: host.example.com
      port: 12345
      user: foobar
      pass: foobar
      dbname: foobar_dev
      schema: foobarmodels
